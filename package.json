{
  "name": "md-renderer",
  "version": "0.0.0",
  "private": true,
  "description": "Portable, browser-based Markdown renderer + AI chat frontend",
  "license": "GPL-3.0",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "demo": "tsx examples/demo.ts"
  },
  "devDependencies": {
    "jsdom": "^25.0.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
